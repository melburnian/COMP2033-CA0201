package ds.fruit;

public interface FruitComparatorInterface {

	/**
	 * Compares two fruits.
	 *
	 * @param f1 the first fruit to compare
	 * @param f2 the second fruit to compare
	 */
	void compare(Fruit f1, Fruit f2);

}